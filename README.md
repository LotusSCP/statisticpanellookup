# statisticpanellookup
a player statistic lookup (like login but only username) "."."  (name).(domain name).(level domain)  Name = (name)  domain name (leave a blank if its empty) = (domain name like neocities, netlify)  level domain (like .com, .app, .io) = (level domain like .com, .app, .io)
